<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ch.zhaw.mdp.lhb.citr"
    android:versionCode="1"
    android:versionName="1.0-SNAPSHOT" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="18" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/citr"
        android:label="Citr"
        android:theme="@style/AppTheme" >

        <activity android:name=".activities.ListViewLoader" android:label="Citr">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activities.LoginActivity" android:label="Citr">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activities.MainActivity"
          android:label="Citr"
          android:parentActivityName="Login"
        >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>

        <activity android:name=".activities.GroupCreateActivity"
          android:label="Gruppe erstellen"
          android:parentActivityName="Main"
        >
        </activity>

        <activity android:name=".activities.CitrCreateActivity"
                  android:label="Citr veröffentlichen"
                  android:parentActivityName="Main"
                >
        </activity>

        <activity android:name=".activities.GroupList"
                  android:label="Gruppe suchen"
                  android:parentActivityName="Main"
                >
        </activity>
        
         <activity
            android:name=".activities.ErrorActivity"
            android:label="@string/app_name" >
        </activity>
    </application>

</manifest>